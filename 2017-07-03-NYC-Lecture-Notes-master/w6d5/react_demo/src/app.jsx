import React from 'react';
import CatIndex from './cat_index';

const App = (props) => {
  // props come from above, props are sacred,
  // DO NOT mutate props
  return <CatIndex />;
};

export default App;
