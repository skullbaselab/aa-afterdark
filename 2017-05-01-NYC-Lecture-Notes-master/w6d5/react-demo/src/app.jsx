import React from 'react';
import CatIndex from './cat_index';

const App = (props) => (
    <section>
      <h1>CATS!</h1>
      <CatIndex />
    </section>
  );

// exports ONE thing, use the default keyword
export default App;
