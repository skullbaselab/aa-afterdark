import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

const root = document.getElementById('content');

//puts the app component on the page inside root element
ReactDOM.render(<App />, root);
