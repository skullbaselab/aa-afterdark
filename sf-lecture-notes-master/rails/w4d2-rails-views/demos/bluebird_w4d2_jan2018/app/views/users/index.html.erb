<h1>Check out how many users we have now</h1>
<%= link_to "Create New User", new_user_url %>

<% @users.each do |user| %>
  <!-- Render a partial for a user -->
  <%= render 'user', user: user, action: :index %>
<% end %>
